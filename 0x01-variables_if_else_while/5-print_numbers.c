#include <stdio.h>

/**
 * main - Prints all single digit numbers in base 10 starting from 0
 * Return: 0 always
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
	printf("%d", i);
	}
	printf("\n");
	return (0);
}
